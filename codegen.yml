overwrite: true
schema: "lib/schema.graphql"
documents: "components/**/*.graphql"
generates:
  lib/generated.ts:
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
      - typescript-msw
  ./graphql.schema.json:
    plugins:
      - introspection
