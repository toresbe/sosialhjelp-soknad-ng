type Soknad {
    id: ID!
    telefon: TelefonData
}

type TelefonData {
    id: ID!
    fraKrr: String
    brukerdefinert: String
}

type Query {
    soknad(id: ID!): Soknad
}

type Mutation {
    opprettSoknad: Soknad!
    setTelefonnummer(id: ID!, tlfnr: String): Soknad!
}

